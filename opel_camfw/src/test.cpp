#include "cam_core.h"
#include <iostream>

#define errExit(msg) do {perror(msg); exit(EXIT_FAILURE);}while(0)
int main()
{
/*	Camera cam;
	if(!cam.open_device("/dev/video0"))
		printf("Fail to open_device");
	
*/	
	return 0;	

}
